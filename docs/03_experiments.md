# 실험 로그

> 규칙: 학습·제출 실험은 **전부** 여기에 기록한다. CV는 5-fold GroupKFold(session_id, seed 42) macro-F1.
> OOF/테스트 확률은 `artifacts/{exp_id}/`에 저장 (threshold 튜닝·앙상블 재료).

## 요약 보드

| exp | 날짜 | 모델/방법 | 입력 | CV (mean±std) | LB | zip | 추론시간 | 비고 |
|---|---|---|---|---|---|---|---|---|
| E000 | 07-12 | TF-IDF+LogReg (주최측 파리티) | prompt만 | 0.42605±0.0044 | **0.43576** | ~8MB | 로컬 수초 | LB는 주최측 zip 그대로(전체데이터 학습). 튜닝 후 CV 0.43734 |
| E001 | 07-12 | TF-IDF+LogReg, class_weight=balanced | full(v1) | 0.49791±0.0034 | | | | 튜닝 후 0.50854. 컨텍스트 +0.07 |
| SMK_MBERT | 07-12 | bert-base-multilingual-cased 2ep | now_first | 0.379 (6k, fold0만) | | | | 환경 판별용 |
| SMK_KLUE | 07-12 | klue/roberta-base 2ep | now_first | 0.130 (6k) | | | | 영어/코드 혼합에 불리 |
| SMK_MDEB | 07-12 | mdeberta-v3-base 2ep | now_first | 0.187 (6k) | | | | 이전 프로젝트에서도 최종 0.68로 열세 |
| (이전) | ~07-09 | **Qwen3-0.6B-Base 3ep** (dacon/) | V2 | fold0 **0.7679** / 5-fold OOF **0.7701** | 0.7677 (fold0+au) | 839MB | 9:31 | 이전 기준선. dacon/PROGRESS.md |
| E100 | 07-13 | Qwen3-0.6B **full-data** 3ep + au | V2 | (홀드아웃 없음) | **0.77124** 🏆 | 841MB | 9:31 | **+0.0035 vs fold0. full-data 레버 실증. 현 기준선** |
| E101 | 07-13 | Qwen3-**Embedding**-0.6B fold0 3ep | V2 | **0.7561** | | | | **기각** (Base 0.7679 대비 −0.0118, 게이트 미달). 에폭 곡선 0.7086→0.7540→0.7561 — 임베딩 후학습이 분류 fine-tuning 출발점으로 불리 |
| E102 | 07-13 | **Qwen3-1.7B LoRA 교사** fold0 (r32 all-linear, lr1e-4, 3ep, 6.5h) | V2 | **0.7516** | | | | **기각** (게이트 0.785 미달, 0.6B full-FT보다 −0.016). 곡선 0.7074→0.7503→0.7516 + e3 loss 상승 = 수렴 후 정체. XLM-R-large(−0.006)와 동일 패턴 — **모델 확대는 이 태스크에서 통하지 않음(2회 재현)** |
| E103 | 07-13 | 0.6B full-data **OOF 다중교사 증류** (teacher=Qwen3×XLM-R OOF 블렌드 0.7735, T=3, α=0.6) | V2 | (홀드아웃 없음) | **0.77264** | 841MB | | +0.0014 vs E100. 앙상블 지식의 단일 모델 압축 실증 |
| E105 | 07-14 | 0.6B full-data **3-way 교사 재증류** (teacher=Qwen3/XLM-R/mmBERT OOF 블렌드 0.7754, T=3, α=0.6) | V2 | (홀드아웃 없음) | **0.77326** 🏆 | 841MB | | **+0.0006 vs E103. 교사 다양화(+mmBERT) 이득 LB 실증. 현 기준선** |
| E106 | 07-14 | Qwen3-0.6B fold0 **lr 4e-5** 프로브 | V2 | fold0 **0.7633** | | | | **기각** (게이트 0.7699 미달, 기준 lr2e-5 0.7679보다 −0.0046). lr 상향은 Qwen3에서 열세 — XLM-R lr↑ 이득 전이 안 됨 |
| E108 | 07-14 | **au_bias 스케일 스윕** (distill3w 후처리, 재학습 없음) | V2 | — | ×0.7 **0.77374** 🏆 / ×1.0 0.77326 / ×1.3 0.77227 | 841MB | 9:31 | **au_bias 과교정 완화가 이득. 약화 단조 추세 → ×0.5/×0.3 스윕 중. 현 최고** |

## 제출 기록 (일 10회 제한 — 남은 횟수 관리)

| # | 날짜 | exp | LB | 메모 |
|---|---|---|---|---|
| 1 | 07-12 | 주최측 baseline zip 그대로 | 0.43576 | 스모크 ①. CV(0.426, 80%학습)↔LB(0.436, 100%학습) 오프셋 ~+0.01로 정합 → **CV 신뢰 가능** |
| 2 | 07-13 새벽 | E100 submit_full3ep.zip | 0.7712407954 (9:31) | full-data +0.0035 실증 |
| 3 | 07-13 오후 | E103 submit_distill.zip | **0.7726378118** | 증류 +0.0014. 누적: 0.7677→0.7726 (+0.0049). 본선컷까지 −0.0204 |
| 4 | 07-13 저녁 | E104 submit_2way.zip (Qwen distill + mmBERT fold0, w=0.5) | **시간 초과 실패** | zip 0.993GB는 통과. 블렌드 CV 0.7738이나 서버 10분 벽 재확인 — 프루닝 없인 2모델 불가 확정 |
| 5 | 07-14 오전 | E105 submit_distill3w.zip (3-way 교사 재증류) | **0.7732569963** | +0.0006 vs E103. 누적: 0.7677→0.7733 (+0.0056) |
| 6 | 07-14 저녁 | E108 submit_distill_au07.zip (distill3w + au_bias ×0.7) | **0.7737444649** 🏆 | **+0.00049 vs ×1.0. au_bias 과교정 완화가 이득. 새 최종 후보** |
| 7 | 07-14 저녁 | E108 submit_distill_au13.zip (distill3w + au_bias ×1.3) | 0.7722637513 | −0.00099 vs ×1.0. au_bias 강화는 악화 — 약화 방향 확정 |

## 실험 상세

### E000 — 주최측 베이스라인 파리티 (prompt-only TF-IDF+LogReg)
- 구성: TF-IDF word(1,2)+char_wb(2,4) → LogReg(C=1), 5-fold GroupKFold. 주최측 pkl 그대로가 아니라 우리 CV로 재학습한 파리티 버전
- CV: [0.43247, 0.42185, 0.43011, 0.42175, 0.42407] → **0.42605 ± 0.0044**
- fold-out threshold 튜닝: **0.43734 ± 0.0010** (+0.011 — 튜닝 파이프라인 동작 확인, 선형 모델에서도 유의미한 이득)
- 결론: prompt 단독은 0.43 수준. 리더보드 0.79와의 갭 = 컨텍스트(history/meta) + 강한 인코더의 몫

### E001 — full-context TF-IDF+LogReg
- 구성: E000과 동일 + 입력을 serialize(full)로 (META+히스토리 6쌍+NOW), class_weight=balanced
- CV: **0.49791 ± 0.0034**, fold-out threshold 튜닝 후 **0.50854 ± 0.0045**
- 결론: 컨텍스트(history+meta)가 선형 모델에서도 +0.07. 남은 갭(0.51→0.79)은 인코더 표현력의 몫

### 디버깅 기록 (07-12 밤) — "트랜스포머가 학습 안 됨" 사건
- 증상: MiniLM/xlm-roberta가 512샘플 암기도 실패 (랜덤 레이블 컨트롤 acc 9%), 실데이터 6k에서 F1 0.04
- 격리 과정: 순수 torch MLP ✓ / 순수 torch TransformerEncoder ✓ / HF 포워드 CPU=GPU ✓ / 옵티마이저-파라미터 identity ✓ / fp32·bf16 동일 / eager·sdpa 동일 / transformers 5.13→4.51.3 다운그레이드해도 동일
- 판별: **mbert(자체 토크나이저)는 정상 학습 궤도, XLM-R계 250k SPM 토크나이저 모델(MiniLM·xlm-r)만 사망** → 원인 완전 규명 대신 "이 환경에서 학습되는 백본 선택"으로 전환 (SMK_* 3파전)
- 교훈: 새 백본은 반드시 512샘플 암기 스모크부터 (train_hf.py --limit 512 --epochs 10)

### SMK_* — 백본 3파전 (6k 샘플, 2ep, 07-12 밤)
- mbert-cased **0.379** / mdeberta 0.187 / klue-roberta 0.130 (동일 조건). mbert만 정상 학습 곡선.
- 단, 직후 이전 프로젝트(dacon/) 발견으로 **인코더 노선 자체가 폐기** — Qwen3-0.6B(0.7679)가 모든 인코더를 +0.03 이상 상회함이 이미 실측돼 있었음. 이 레이스는 "이 환경에서 학습되는 모델" 판별용 기록으로만 의미.

### E100 — Qwen3-0.6B full-data 재학습 (진행 중, 07-12 밤 발사)
- 가설: 이전 프로젝트 제출은 전부 80% fold 모델 → 100% 데이터 재학습으로 +0.003~0.008
- 구성: dacon/work/train_full.py --tag qwen3_full3ep (검증 레시피 그대로, eval 없음, 3ep cosine). 6,561스텝, 저장 `dacon/artifacts/models/qwen3_full3ep_full_best`
- 근거: E000에서 CV(80%) 0.426 vs 주최측 zip(100%) LB 0.436 오프셋 실측, Qwen CV↔LB 갭 0
- 주의: full-data 모델은 정직한 CV가 없음 — 판정은 서버 LB로만.
- 결과 (07-13 새벽 제출): **LB 0.7712407954, 런타임 9:31** — fold0(0.7677) 대비 **+0.0035**, 예상 범위(+0.003~0.008) 하단 적중. 신기록, 현 기준선. 프루닝 후 905MB/zip 841MB, 로짓 동치 max|Δ|=0

### R101~R103 — Qwen3×XLM-R 결합 한계 실험 (07-12 저녁, GPT 세션 · OOF 기반 CPU)
- 배경: oracle 선택기(둘 중 하나라도 맞으면 정답) macro **0.8028** / either-correct 80.06% — 다양성 안에 0.80 존재 (07-12 재현 검증 완료)
- R101 2단계 로지스틱 스태킹(logits+turn/meta, 그룹 CV): **0.7709 (+0.0008)** — 기각
- R102 Qwen 예측 클래스 조건부 XLM-R 재판정: 최고 조합 **+0.00004** — 기각
- R103 TF-IDF char3-5+LinearSVC nav 전문가 오버라이드: **0.7679 → 0.7208** — 기각 (표면 전문가가 문맥 정답 파괴)
- 결론: 결합·후처리로는 oracle 회수 불가 → 더 강한 단일 표현(1.7B 교사 증류) 경로 채택. 주의: 이 실험들은 스크립트 미보존(재현 시 재작성 필요)

### R104 — 테스트 분포 시프트 후처리 검증 (07-13 새벽, CPU)
- 배경: 상위팀 제출수 54~86회 → LB-가이드 후처리 튜닝 가설. 우리는 LB 대신 의사-테스트로 검증
- 프로토콜: OOF에서 "세션당 1스텝 + au 15%" 의사-테스트 구성, 세션 분리 A(튜닝)/B(홀드아웃)
- 결과 (B 홀드아웃, 10 draws): EM prior 적응 **−0.009** (방향 감지는 정확하나 prior-매칭이 F1 파괴) · 전역 F1-bias **−0.0003** · turn-조건부 **+0.0001** · **au-조건부 +0.0055** (유일 생존 — 기존 au_bias와 동일 수치, 이미 zip 탑재분)
- 결론: 시프트-후처리 헤드룸은 au로 이미 소진. 상위팀 제출수는 앙상블 가중/모델 반복으로 추정. 스크립트: src/test_shift_adapt.py, src/test_shift_bias.py

### E104 — mmBERT 5-fold + 블렌드 판정 (07-13 저녁)
- mmBERT-base 5-fold: [0.7582, 0.7694, 0.7602, 0.7634, 0.7664] → **OOF 0.7636** (XLM-R 0.7348 대비 +0.029 — 신세대 인코더 실증)
- 블렌드 OOF: Qwen×mmBERT(w=.5) **0.7738** / 3-way(q.40/x.27/m.33) **0.7754** — 둘 다 CV로는 기준(0.7735) 상회
- 제출 실측: 2-way(0.993GB) **서버 10분 초과 실패** → 블렌드는 Qwen 레이어 프루닝 없이 물리적 불가 확정. 프루닝 경로는 정확도·시간 이중 리스크 대비 기대 +0.001~2라 보류
- 부산물: mmBERT 프루닝 237MB(로짓 동일), 인코더 서버 추론 ~3분급 — 향후 인코더-단독 계열 카드의 기반

### R105~R106 — CPU 헤드룸 판정 2건 (07-13 밤, GPU 절약)
- R105 **result_summary 상태 태그 기각**: ERR/ZERO 상태 구간의 Qwen acc가 전체 평균보다 높음(ERR 0.791, ZERO 0.803, grep-0건 0.875 vs 전체 0.765) — 상태 신호는 이미 완전 소화. 진짜 약점(NOHIST 0.576, list_directory직후 0.579)은 태그로 안 풀리는 정보 한계. verify-cue 패턴 3번째 재현. 스크립트 src/rs_headroom.py
- R106 **turn-1 메타 보정기 기각**: 메타-전용 GBDT acc 0.239(Qwen 0.576), fold-정직 블렌드 전체 환산 **+0.0004** — 노이즈 수준. 메타는 직렬화 텍스트로 이미 흡수됨. 첫 턴 = 정보 한계 재확인. 스크립트 src/turn1_meta_headroom.py
- 교훈: "모델이 텍스트로 이미 읽는 신호"는 어떤 형태의 명시화·보조 모델로도 이득이 안 남 (4번째 재현)

### E105 — 3-way 교사 재증류 + BSM 게이트 (07-13 밤 체인, 07-14 판정)
- 슬롯 A: 3-way 교사(0.7754) 재증류 full-data **완주** → 프루닝(로짓 동치)·패키징·드라이런 통과, `da2\submit_distill3w.zip` (841MB). **LB 0.7732569963** (07-14 제출#5) — E103(0.77264) 대비 **+0.0006**. 교사 다양화(Qwen3×XLM-R → +mmBERT)의 LB 이득 실증. CV에선 3-way 0.7754 > 2-way 0.7738(+0.0016)이었고 LB 방향 일치(축소 전이). **새 기준선·최종 후보**
- 슬롯 B: **balanced softmax 기각** — fold0 **0.7595** (0.714→0.758→0.760), 기준 0.7679 대비 **−0.008**. 마지막 미검증 학습 레버 사망. sqrt CE가 이 태스크 최적으로 확정
- 사고 기록: 자동 패키징이 MODEL_WEIGHTS 잔재(2-way [0.5,0.5])로 assert 실패 → 아침에 수동 복구. 교훈: 공유 스크립트의 EDIT-ME 블록은 실험 후 즉시 원복

### E106 — lr 4e-5 fold0 프로브 (07-14, 기각)
- 배경: 오차 감사에서 "미검증 드롭" 판정된 카드. XLM-R에선 lr 2e-5→4e-5가 +0.0098 (이전 프로젝트 #14)
- 구성: qwen3_smoke 레시피에서 lr만 4e-5. 게이트: fold0 ≥ 0.7699 (기준 0.7679)
- 곡선(eval_macro_f1): e1 0.6912 → e2 0.7595 → **e3 0.7633** (best). **게이트 0.7699 미달(−0.0066)**, 기준 lr 2e-5(0.7679)보다도 **−0.0046**
- 결론: **기각**. lr 상향은 Qwen3에서 열세 — XLM-R의 lr↑ 이득(+0.0098)이 전이 안 됨. "오차 감사 미검증 드롭 3건" 중 lr 소진. 오후 최종 재학습 후보였던 "lr4e-5+3way 증류" 무효 → 결정 트리 실패 경로로 전환

### E107 — 혼동집합 조건부 CE (07-14 오후, 진행 중)
- 가설: 그룹 내부 경계(특히 nav 4-way: glob/grep/list/read)를 보조손실로 선명화 → macro-F1 +0.002~0.005. 추론 비용 0 (헤드 불변, 학습 그래디언트만 변형)
- 변경점: `L = CE14 + λ·CE_group`, λ=0.3. CE_group = 정답이 속한 혼동그룹 내부만 재-softmax한 (class-weighted) NLL. 그룹: nav(glob/grep/list/read)·verify(lint/run_bash/run_tests)·dialogue(ask/plan/respond)·modify(apply/edit/write)·web(web_search 단독→기여 0)
- 구현: `da2/src/train_group_ce.py` (dacon/work/train.py 포크, GroupCETrainer.compute_loss에 마스킹 NLL 추가). 마스킹·nll 로직 단위검증 통과(web 단독그룹 nll=0, 전부 유한). 배경: lr 실패로 오후 슬롯 재배정, 사용자 선택
- 구성: Qwen3-0.6B-Base fold0, 검증 레시피(max_len512·3ep·bs8ga4·lr2e-5·sqrt·bf16·adamw_bnb_8bit·seed42) + group_ce_lambda 0.3. 게이트 fold0 ≥ 0.7699 (기준 0.7679)
- 곡선(eval_macro_f1): e1 0.7155 → **e2 0.7634 (best)** → e3 0.7597. **게이트 0.7699 미달(−0.0065)**, 기준 0.7679 대비 **−0.0045**
- 결론: **기각**. e3 하락 = group CE(λ0.3) 과조정 신호. R107 진단대로 "큰 병목=초기 nav 정보한계"라 그룹경계 보조손실이 순해로움. full-data 미발사, distill3w(0.77326) 확정. λ 튜닝(0.2) 여지는 있으나 진단상 상방 작음 — 오늘밤 full-data 시간 초과로 보류

### R107 — 오차 상보성 진단 (07-14 오후, CPU, GPU 학습 중 병행)
- 목적: 계획서 지목 "미해결 최유력 지점" — oracle(3-way 0.8168)의 상보성이 어느 축에서 오는가. Qwen3/XLM-R/mmBERT 5-fold OOF(70k) 분해. 스크립트 `src/complementarity_diag.py`
- **소재 = nav 그룹**: nav(glob/grep/list/read)가 전체의 41%(n=28782), Qwen acc **0.590**로 macro 최대 병목. 구제 여지(rescue)도 nav 집중(0.089). modify/respond/write는 포화(0.98~1.0, 상보성 0)
- **오답 94.4%가 그룹내 혼동** (15506/16431) → E107 group CE 설계가 실제 오차구조와 정합. 그룹밖 오답 5.6%뿐
- **정보 한계 확정**: nav 오답이 초기 스텝 집중 — step01 acc **0.410** → step02 0.495 → step3-5 0.638 → step6+ 0.677. **블렌드가 초기 스텝을 못 고침**(step01 blend 0.404 < Qwen 0.410, 세 모델 공통 오답). 히스토리 없는 첫 스텝은 read/grep/glob/list 구분 단서 자체가 없음
- **블렌드 상한 규명**: nav blend_acc 0.591 ≈ Qwen 0.590. 블렌드 OOF **0.7734 ≈ distill3w LB 0.77326** — 증류 학생이 교사(블렌드)를 완전 흡수. 상위팀 +0.02는 정보한계 구간(초기 nav)에서 나올 수 없음
- **au 정정 재확인**: au Qwen acc 0.853 > sim 0.758, 블렌드는 au에서 역효과(0.812). au_bias 유지·블렌드 배제 정당
- **함의**: E107 group CE 설계는 타당하나 상한 제한적 예상(큰 병목=정보한계 초기 nav는 그룹경계 선명화로도 불가). 후기 스텝 nav(0.64~0.68)에서만 여지. 목표 0.80/본선컷 미달 전망 재확인. fold0 실측이 최종 판정

### E108 — au_bias 스케일 스윕 (07-14 저녁, 후처리·재학습 없음)
- 배경: R107 진단에서 au 세그먼트는 Qwen(증류 학생) acc 0.853로 이미 강함, 블렌드 역효과. 기존 au_bias(×1.0, 이전 프로젝트 XLM-R 시절 튜닝값)가 distill3w엔 과교정 가설
- 방법: distill3w 모델 고정, `au_bias.json`의 au_bias_low/high만 스칼라 배. 패키징만(CPU), 추론시간·모델 동일. 스크립트 scratchpad/au_scale_probe.py (원본 해시 원복 포함)
- LB: **×0.7 = 0.7737444649** (+0.00049) / ×1.0 = 0.7732569963 / ×1.3 = 0.7722637513 (−0.00099)
- 결론: **au_bias 약화가 이득, 단조 추세**. ×0.7이 현 최고(`submit_distill_au07.zip`). ×0.5/×0.3 추가 스윕으로 최적점 확인 중. 주의: au_bias 완전 제거(×0)는 이전 LB +0.0055 실측과 배치되므로 U자 최적 가능 — 스윕으로 판별
- 함의: 후처리 소진 결론(R104) 재검토 — au_bias는 살아있으나 **강도**가 모델 세대에 종속적이었음

### 템플릿 (복사용)

### EXXX — 제목
- 가설:
- 변경점 (vs 직전):
- 구성: 모델 / 입력 직렬화 / max_len / epochs / lr / class weight / seed
- CV: fold별 [ , , , , ] → mean±std
- threshold 튜닝 후 CV:
- LB:
- 결론 / 다음 액션:
